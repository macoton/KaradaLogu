# KaradaLogu

シンプルなHTML/JavaScriptアプリケーション。

## 機能

- 入力画面: 
  - テキストボックスに数字を入力し、**Enterキー**で記録
  - 全角数字（例：１２３）は自動的に半角（123）に変換されます
  - 3桁の数字を入力すると、入力値÷10を体温として記録
  - 9桁の数字を入力すると、前3桁を最高血圧、次3桁を最低血圧、最後3桁を脈拍として記録
  - その他の文字列（数字以外）を入力すると「メモ」として日時と共に保存され、出力画面にボタンとして一覧表示されます。ボタンを押すだけで同じメモが再度記録されます。

- 出力画面: 記録した体温・血圧・脈拍の一覧を表示
- 設定画面: データを全て削除

## 実行方法

1. リポジトリをクローン
2. `index.html` をブラウザで開く

データはブラウザの `localStorage` に保存されます。